# V0.4.1.3 P3a-C2 Performance STOP Contract Correction

## Status and scope

This append-only plan responds to V0.4.1.2 section 12.5. It freezes the safety, oracle, Pilot, and authorization contract for a later C2P-FIX; it implements nothing. Base is `main@b70bcfd0f84b77db78fffbc783410284dfad10ac`. PR #41 H `3c8d40d6c04113845ad7fa6a776cb500e3834c94` (tree `ed4d5224f37699edfb96e262476fdf687ba2ff9d`) is immutable failure evidence.

The first formal 66-cell run produced 60 atomic results in three hours: all stopped at about 180 seconds with timeout-derived `rss_unavailable` and `contract_error`; no E, summary, or GO exists. C2D directionally observed READY 0.156s, micro supervisor 8.812s completed, and formal coverage/0.5x/dense-crossing/seed 41: generation 50.875s, feature 0.329s, per-asset validation 50.53s, top-level composer 103.515s, worker 205.250s, supervisor timeout 180.016s. D2/D3/D4 jointly consume time and D4 is largest; scope/conflict root cause is not proven. The former failure directory was observed as 60 JSON/0 tmp/0 other, but no persisted before/after six-tuple comparison exists: immutability is **unverified**.

## Frozen invariants

Keep wall limits 60s at 0.5x/1.0x/1.5x and 120s at 2.0x; RSS 512MiB/1024MiB; 2x/1x wall-median ratio <=6, RSS ratio <=3, output-JSON ratio <=3; 180s hang guard; 3h reference budget. Generation seed is 41; permutation seeds are `[7,11,19,23,31]`; coverage is one run/cell, performance is one warmup plus three measured/cell, and determinism is one run/seed. Input basis is `canonical_benchmark_input_json_v1`; output basis is `canonical_composition_report_plus_final_profile_if_present_v1`. The matrix is 66 = 16 coverage + 10 performance + 40 determinism; the worker shape is 104 = 16 + 40 + 40 + 8 baselines. Schema, artifact contracts, canonical bytes/fingerprints, terminal traces, metrics, coverage conservation, stable IDs, fail-closed rejection, disabled/non-delivery intent, and runtime zero-import remain unchanged. No sampling, omitted assets/rules/bindings/candidates/conflicts/scopes, altered timing basis, or relaxed threshold may manufacture a pass.

## Git and authorization DAG

PLAN is docs-only from this base. After independent review and explicit user merge approval, C2P-FIX starts from the PLAN merge SHA, mechanically records H-vs-main provenance, and creates a new Draft H2 candidate. PR #41 is closed/replaced only by separate user decision. Any H2 code change invalidates Pilot. C2P-FIX, Pilot, formal RERUN from an empty new directory, E, Ready, merge, PR #41 state, and P3a-R each require separate authorization. Old H STOP files are archive-only and cannot be resumed, overwritten, or converted to GO.

## Safe optimization and oracle

Composer direct calls remain independently fail-closed. Reuse order is mandatory: first copy/deep-freeze caller input, then validate that snapshot, then never read the original object again; fingerprints, inventory, candidate/report/final work all consume the same snapshot, bound to its content plus registry/schema/authority digests. Any validator cache is immutable, content-addressed, process-local; semantic reuse is one-call bounded and never cross-subject/campaign. Runner may retain separate validation timing but cannot remove composer boundary validation or hide relocated work. Generation may remove duplicate work only. First profile outside artifacts, then make minimal changes.

Before any FIX code change, an isolated H worktree must attempt exactly once a non-campaign 0.5x/dense-crossing worker under 240s and persist non-artifact request/H/tree/subject/environment/exit/wall/RSS plus canonical bytes-or-digests, fingerprints, terminal trace, metrics, conservation and stable IDs. Timeout is `baseline_unavailable`, never retried. It does not block profiling, but blocks FIX user-merge consideration until an independent old-H oracle or mechanical equivalence proof exists. Other formal cells may claim only schema/terminal/metrics/conservation/stable-ID invariants, never byte equality. Expected and actual must not come from the same new implementation.

Old H failure directories require start/end per-file `(name,type,bytes,raw SHA256,mtime)` equality; absent start evidence is reported unverified. New Pilot/RERUN directories begin empty; each phase persists a manifest, uses canonical-prefix append-only atomic writes, and its final manifest must mechanically equal the actual directory. Profiling evidence is non-artifact and binds commit/tree/environment/command/request/manifest/exit/wall/RSS/exception without identities, absolute paths, or manuscript content.

## Pilot contract

All pilots use one exact H2, frozen environment, original supervisor, and non-E directory; identical A/B evidence counts toward C. A is `0.5x/dense-crossing`, seed 41/no permutation: completed, `unresolvable`, no final, <=60s/512MiB, and no timeout/RSS/contract error/drift. B requires `1.0x/mixed-conflict-approval` to trace pre-approval `awaiting_approval`, runner-generated-and-applied approval, then post-approval/terminal `final`, final present, and `final_requirement=conditional`; `1.5x/disjoint` = final/final-required; `2.0x/subset-chain` = final/final-required, with 60/120/RSS gates. C runs all 16 coverage; performance is the `1.0x/mixed-conflict-approval` and `2.0x/mixed-conflict-approval` pair, one warmup plus three measured; determinism is `1.5x/dense-crossing` and `2.0x/mixed-conflict-approval`, seed 31 against permutation-none baselines; validates all ratios. The 104-call formula is 16+40+40+8. Define the only extrapolation as `U(scale) = max(end_to_end_wall of every successful Pilot C child at scale)` and `T_upper = sum(U(scale_i) for each of the 104 formal invocations)`, counting every performance child separately (four/cell) and all eight determinism baseline calls separately; end-to-end includes process start and supervisor overhead. Only `T_upper <= 2.4h` passes. RSS/output ratios remain computed from actual paired 1x/2x performance data; extrapolation never substitutes for ratio gates. Stop budgets: A 3m; A+B 12m; A+B+C 45m. Any STOP/overrun ends progression. Pilot only permits requesting RERUN.

## Future FIX boundary

Carry-only H files are byte-identical and never editable: `format-monograph/references/benchmarks/v0412/p3a-c2/benchmark-config.json`, `format-monograph/references/benchmarks/v0412/p3a-c2/projected-envelope.json`, `format-monograph/scripts/profile_v2_benchmark_reference.py`, and `tests/test_v0412_p3a_c2c_reference.py`; any blob drift is STOP. Editable files are `format-monograph/scripts/profile_v2_composer.py`, `format-monograph/scripts/profile_v2_benchmark_runner.py`, `tests/test_v0412_p3a_c2b_runner.py`, one new formal-scale performance test, and approved new fixtures only. `profile_v2_artifacts.py` is conditional on profiling proof. `profile_v2_canonical.py` or `profile_v2_registry.py` require contract-gap plus user approval. Schemas, frozen config and existing fixtures, SKILL/runtime, old 60 results, and unrelated files are forbidden.

## Exit and non-claims

PLAN requires linked INDEX, zero edits to old plans, whitelist-only diff, diff check, risks/rollback/evidence gaps and authorization points. It may claim only “performance STOP revision plan candidate formed”, never a repair, H2, Pilot/RERUN/E, C2 completion, PR #41 mergeability, P3a-R, enablement, or release.

## Residual risks and evidence gaps

D4 is only the largest top-level observation; an algorithmic root cause is unproven. Old-H six-tuple before/after evidence is missing. The byte oracle covers only the fixed first formal cell; other formal cells remain invariant-only. Pilot extrapolation is not a formal result, and PLAN itself repairs no performance behavior.

## Rollback and stop

PLAN rollback removes only 0.4.1.3 and its INDEX row. FIX/H2/Pilot/RERUN each roll back through their own PR and external evidence, never old plans or the old 60 STOP files. Any need to change a frozen threshold, schema/config, code whitelist, or any failure to obtain the required formal oracle is a STOP and manager report.
